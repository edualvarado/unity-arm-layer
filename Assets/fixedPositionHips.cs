using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class fixedPositionHips : MonoBehaviour
{
    [SerializeField] private Transform fixedHips;

    // Start is called before the first frame update
    void Start()
    {
        //this.transform.position = fixedHips.position;
    }

    // Update is called once per frame
    void Update()
    {
        //this.transform.position = fixedHips.position;
    }
}
